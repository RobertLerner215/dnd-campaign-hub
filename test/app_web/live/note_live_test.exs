defmodule AppWeb.NoteLiveTest do
  use AppWeb.ConnCase, async: true

  @moduledoc """
  These generated note LiveView tests were replaced by the final D&D note tests.

  The final project uses /dnd/notes with role-based DM/player visibility,
  not the old generated /notes routes.
  """
end

defmodule AppWeb.DndLive.NoteLiveTest do
  use AppWeb.ConnCase, async: true

  @moduledoc """
  These generated D&D note LiveView tests used old routes like /dnd_live/notes.

  The final project uses /dnd/notes with authentication and role-based note
  visibility. Final-project note behavior is tested in App.DndVisibilityTest.
  """
end

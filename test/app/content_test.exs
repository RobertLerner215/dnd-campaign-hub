defmodule App.ContentTest do
  use App.DataCase, async: true

  @moduledoc """
  These older generated content tests expected scoped content behavior.

  The current project keeps content pages public/simple, so final-project testing
  is focused on the D&D campaign hub features.
  """
end
